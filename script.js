const swiper = new Swiper('.swiper', {
    loop: true,
    // autoplay: true,
    // delay: 2000,
    speed: 1000,
    allowTouchMove: false,

    // Navigation arrows
    navigation: {
        nextEl: '.swiper-next',
        prevEl: '.swiper-prev',
    },
})
